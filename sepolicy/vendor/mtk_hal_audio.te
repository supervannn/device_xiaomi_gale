type system_audio_prop, property_type;

get_prop(mtk_hal_audio, system_audio_prop)
set_prop(mtk_hal_audio, system_audio_prop)

allow mtk_hal_audio persist_data_file:dir { search };

allow mtk_hal_audio self:process execmem;

allow mtk_hal_audio system_audio_prop:file { getattr open read map };
